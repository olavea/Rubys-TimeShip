Let’s talk about what doing a dev-task again && again && again is like for a plugin pirate.
And how you can automate that ONE dev-task in your dev-brain by doing it even more times again && again && again.
